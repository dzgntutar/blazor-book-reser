@inherits BaseButtonComponent

<ButtonComponent Text="@(Text ?? "Add")"
				 Color="@(Color ?? "Primary")"
				 Icon="@(Icon ?? "plus-square")"
				 OnClick="OnClick"
				 ToolTip="@(ToolTip ?? "Add")"
				 IsOutline = "IsOutline"
				 IsSmall = "IsSmall"
></ButtonComponent>

